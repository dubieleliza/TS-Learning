// Type Annotations Practice
//Create variables with explicit types:
// Create a variable called `projectName` with type string.
//Create a variable called `testCount` with type number.
// Create a variable called `isTestCompleted` with type boolean.
// Print all variables to the console.

let projectName: string = "TS for begginer";
let testCount: number = 4;
let isTestCompleted:boolean = true; 


console.log(projectName);
console.log(testCount);
console.log(isTestCompleted);

