// 1 Create variables for a user:
//userName → user's name (string)
//userAge → user's age (number)
//isActiveUser → whether the user is active (boolean)
//Print all values using console.log().

let userName: string = "Eliza";
let userAge: number = 25;
let isActiveUser: boolean = true; 

console.log(userName);
console.log(userAge);
console.log(isActiveUser);

//Create variables for browser test configuration:
//browserName → browser name (string)
//browserVersion → browser version (string)
//timeout → test timeout in milliseconds (number)
//isHeadlessMode → whether browser runs in headless mode (boolean)
//Print all values

let browserName: string = "Chrome";
let browserVersion: number = 1.2;
let timeout: number = 5000;
let isHeadVisible: boolean = false;

console.log(browserName);
console.log(browserVersion);
console.log(timeout);
console.log(isHeadVisible);

//he following code has type mistakes. Correct it:
//let username: string = 123;
//let age: number = "twenty five";
//let isLoggedIn: boolean = "yes";

let username: string = Agnes;
let age: number = "25";
let isLoggedIn: boolean = "true";